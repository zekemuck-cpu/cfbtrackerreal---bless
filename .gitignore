node_modules
dist
.env
.env.local
.DS_Store
.claude/worktrees/
.claude/scheduled_tasks.lock

# User-specific spreadsheets / migration outputs (one-off use, not for repo)
*.xlsx
migrated-dynasty.json

# Local Playwright preview artifacts (Claude Code MCP)
.playwright-mcp/
*.png
# …but the bundled UI assets (imported by components) MUST be tracked.
!src/assets/**/*.png
!public/**/*.png

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Brainstorming visual-companion artifacts
.superpowers/

# CFB27 PC save-import artifacts — never commit
scratch_ro27_asar/
firebase-debug.log
firestore-debug.log
# 800MB portrait library: served from CDN, not the repo
public/cfb27-portraits/

# Windows temp/scratch artifacts that have been accidentally committed before
# (a 32 MB c27_schema.xml landed in PC V4 with the whole temp path as its name).
Users*AppData*Temp*
*c27_schema.xml
